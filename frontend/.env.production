# Production API URL - update this for your production environment
# When using nginx proxy, API calls will be proxied through the same domain
# Empty string because services already include /api/ prefix in their paths
VITE_API_BASE_URL=
